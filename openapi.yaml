components:
  schemas:
    AuthenticatedUser:
      type: object
      properties:
        email:
          type: string
          format: email
          pattern: ^(?!\.)(?!.*\.\.)([A-Za-z0-9_'+\-\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\-]*\.)+[A-Za-z]{2,}$
        id:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        token:
          type: string
          description: Bearer token, valid seven days.
      required:
        - email
        - id
        - token
      additionalProperties: false
      id: AuthenticatedUser
    Category:
      type: object
      properties:
        id:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        label:
          type: string
        photoCount:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        slug:
          type: string
        sortOrder:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
      required:
        - id
        - label
        - slug
        - sortOrder
      additionalProperties: false
      id: Category
    CategoryCreate:
      type: object
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 120
        sortOrder:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
      required:
        - label
        - sortOrder
      additionalProperties: false
      id: CategoryCreate
    Credentials:
      type: object
      properties:
        email:
          type: string
          format: email
          pattern: ^(?!\.)(?!.*\.\.)([A-Za-z0-9_'+\-\.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9\-]*\.)+[A-Za-z]{2,}$
        password:
          type: string
          minLength: 8
      required:
        - email
        - password
      additionalProperties: false
      id: Credentials
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required:
        - error
      additionalProperties: false
      description: Every failing endpoint answers with this shape.
    Page:
      type: object
      properties:
        content:
          description: TipTap document.
        createdAt:
          type: string
          format: date-time
          pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
        icon:
          anyOf:
            - type: string
            - type: "null"
        id:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        order:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        slug:
          type: string
        status:
          type: string
          enum:
            - draft
            - published
        title:
          type: string
        updatedAt:
          type: string
          format: date-time
          pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
      required:
        - content
        - createdAt
        - icon
        - id
        - order
        - slug
        - status
        - title
        - updatedAt
      additionalProperties: false
      id: Page
    PageCreate:
      type: object
      properties:
        icon:
          anyOf:
            - type: string
            - type: "null"
        slug:
          type: string
          minLength: 1
          maxLength: 60
        status:
          type: string
          enum:
            - draft
            - published
        title:
          type: string
          minLength: 1
      required:
        - slug
        - title
      additionalProperties: false
      id: PageCreate
    PageSummary:
      type: object
      properties:
        icon:
          anyOf:
            - type: string
            - type: "null"
        id:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        order:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        slug:
          type: string
        title:
          type: string
      required:
        - icon
        - id
        - order
        - slug
        - title
      additionalProperties: false
      id: PageSummary
    Photo:
      type: object
      properties:
        alt:
          type: string
          description: Falls back to the title, so never empty.
        category:
          type: string
          description: URL-safe slug.
        categoryId:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        categoryLabel:
          type: string
        chromeUrl:
          anyOf:
            - type: string
            - type: "null"
          description: Address drawn in the browser chrome. Display only.
        createdAt:
          type: string
          format: date-time
          pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
        exif:
          anyOf:
            - type: object
              properties:
                aperture:
                  type: number
                  exclusiveMinimum: 0
                exposureTime:
                  description: Seconds
                  type: number
                  exclusiveMinimum: 0
                focalLength:
                  description: Millimetres
                  type: number
                  exclusiveMinimum: 0
                iso:
                  type: integer
                  exclusiveMinimum: 0
                  maximum: 9007199254740991
                lens:
                  type: string
                  maxLength: 120
                make:
                  type: string
                  maxLength: 120
                model:
                  type: string
                  maxLength: 120
                takenAt:
                  type: string
                  format: date-time
                  pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
              additionalProperties: false
            - type: "null"
        height:
          anyOf:
            - type: integer
              minimum: -9007199254740991
              maximum: 9007199254740991
            - type: "null"
        id:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        isFeatured:
          type: boolean
          description: Included in the homepage hero.
        isPublished:
          type: boolean
          description: False hides it from the public site.
        lqip:
          anyOf:
            - type: string
            - type: "null"
          description: Inline blurred placeholder.
        order:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        originalUrl:
          anyOf:
            - type: string
            - type: "null"
          description: Pre-edit image, for undo.
        showChrome:
          type: boolean
          description: Frame this image as a browser window.
        title:
          type: string
        url:
          type: string
          format: uri
        width:
          anyOf:
            - type: integer
              minimum: -9007199254740991
              maximum: 9007199254740991
            - type: "null"
      required:
        - alt
        - category
        - categoryId
        - categoryLabel
        - chromeUrl
        - createdAt
        - exif
        - height
        - id
        - isFeatured
        - isPublished
        - lqip
        - order
        - originalUrl
        - showChrome
        - title
        - url
        - width
      additionalProperties: false
      id: Photo
    PhotoCreate:
      type: object
      properties:
        alt:
          type: string
          maxLength: 300
        categoryId:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        exif:
          anyOf:
            - type: object
              properties:
                aperture:
                  type: number
                  exclusiveMinimum: 0
                exposureTime:
                  description: Seconds
                  type: number
                  exclusiveMinimum: 0
                focalLength:
                  description: Millimetres
                  type: number
                  exclusiveMinimum: 0
                iso:
                  type: integer
                  exclusiveMinimum: 0
                  maximum: 9007199254740991
                lens:
                  type: string
                  maxLength: 120
                make:
                  type: string
                  maxLength: 120
                model:
                  type: string
                  maxLength: 120
                takenAt:
                  type: string
                  format: date-time
                  pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
              additionalProperties: false
            - type: "null"
        height:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        lqip:
          type: string
        title:
          type: string
          minLength: 1
          maxLength: 200
        url:
          type: string
          format: uri
        width:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
      required:
        - categoryId
        - title
        - url
      additionalProperties: false
      id: PhotoCreate
    PhotoIdList:
      type: object
      properties:
        photoIds:
          minItems: 1
          type: array
          items:
            type: string
            format: uuid
            pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
      required:
        - photoIds
      additionalProperties: false
      id: PhotoIdList
    PhotoUpdate:
      type: object
      properties:
        categoryId:
          type: string
          format: uuid
          pattern: ^([0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-8][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$
        chromeUrl:
          description: Omit to leave alone; empty string clears it.
          type: string
          maxLength: 300
        exif:
          anyOf:
            - type: object
              properties:
                aperture:
                  type: number
                  exclusiveMinimum: 0
                exposureTime:
                  description: Seconds
                  type: number
                  exclusiveMinimum: 0
                focalLength:
                  description: Millimetres
                  type: number
                  exclusiveMinimum: 0
                iso:
                  type: integer
                  exclusiveMinimum: 0
                  maximum: 9007199254740991
                lens:
                  type: string
                  maxLength: 120
                make:
                  type: string
                  maxLength: 120
                model:
                  type: string
                  maxLength: 120
                takenAt:
                  type: string
                  format: date-time
                  pattern: ^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$
              additionalProperties: false
            - type: "null"
        isFeatured:
          type: boolean
        isPublished:
          type: boolean
        order:
          type: integer
          minimum: -9007199254740991
          maximum: 9007199254740991
        showChrome:
          type: boolean
        title:
          type: string
          minLength: 1
          maxLength: 200
        url:
          type: string
          format: uri
      required:
        - categoryId
        - order
        - title
      additionalProperties: false
      id: PhotoUpdate
    SiteSettings:
      type: object
      properties:
        heroTitle:
          type: string
        instagramHandle:
          type: string
        instagramUrl:
          type: string
        name:
          type: string
        ownerName:
          type: string
        shortName:
          type: string
        showShader:
          type: boolean
        siteKey:
          type: string
        tagline:
          type: string
        theme:
          type: object
          properties:
            accent:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            background:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            foreground:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            sansFont:
              type: string
            serifFont:
              type: string
          required:
            - accent
            - background
            - foreground
            - sansFont
            - serifFont
          additionalProperties: false
      required:
        - heroTitle
        - instagramHandle
        - instagramUrl
        - name
        - ownerName
        - shortName
        - showShader
        - siteKey
        - tagline
        - theme
      additionalProperties: false
      id: SiteSettings
    SiteSettingsUpdate:
      type: object
      properties:
        heroTitle:
          type: string
        instagramHandle:
          type: string
        instagramUrl:
          type: string
        name:
          type: string
        ownerName:
          type: string
        shortName:
          type: string
        showShader:
          type: boolean
        tagline:
          type: string
        theme:
          type: object
          properties:
            accent:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            background:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            foreground:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            sansFont:
              type: string
            serifFont:
              type: string
          required:
            - accent
            - background
            - foreground
            - sansFont
            - serifFont
          additionalProperties: false
      additionalProperties: false
      id: SiteSettingsUpdate
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      scheme: bearer
      type: http
info:
  description: Generated from the handlers in `api/` and the schemas in `schemas/`. Do not edit by hand — run `pnpm openapi`.
  title: Photo portfolio API
  version: 1.0.0
openapi: 3.1.0
paths:
  /api/ai/generate:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_ai_generate
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/ai/generate
      tags:
        - ai
      x-undocumented: true
  /api/ai/icon-webhook:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_ai_icon-webhook
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/ai/icon-webhook
      tags:
        - ai
      x-undocumented: true
  /api/ai/icon:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_ai_icon
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/ai/icon
      tags:
        - ai
      x-undocumented: true
  /api/auth/change-password:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_auth_change-password
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/auth/change-password
      tags:
        - auth
      x-undocumented: true
  /api/auth/forgot-password:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_auth_forgot-password
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/auth/forgot-password
      tags:
        - auth
      x-undocumented: true
  /api/auth/google:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_auth_google
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/auth/google
      tags:
        - auth
      x-undocumented: true
  /api/auth/login:
    post:
      operationId: post_api_auth_login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Credentials"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticatedUser"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: Exchange credentials for a bearer token.
      tags:
        - auth
  /api/auth/me:
    get:
      operationId: get_api_auth_me
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticatedUser"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: The signed-in user.
      tags:
        - auth
  /api/auth/register:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_auth_register
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/auth/register
      tags:
        - auth
      x-undocumented: true
  /api/auth/reset-password:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_auth_reset-password
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/auth/reset-password
      tags:
        - auth
      x-undocumented: true
  /api/boards/{id}:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_boards__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/boards/{id}
      tags:
        - boards
      x-undocumented: true
    patch:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: patch_api_boards__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PATCH /api/boards/{id}
      tags:
        - boards
      x-undocumented: true
    delete:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: delete_api_boards__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: DELETE /api/boards/{id}
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/comments:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_boards__id__comments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/boards/{id}/comments
      tags:
        - boards
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__comments
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/comments
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/comments/{commentId}:
    patch:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: patch_api_boards__id__comments__commentId_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - in: path
          name: commentId
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PATCH /api/boards/{id}/comments/{commentId}
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/dataset:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__dataset
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/dataset
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/export:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__export
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/export
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/run:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__run
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/run
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/svg:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__svg
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/svg
      tags:
        - boards
      x-undocumented: true
  /api/boards/{id}/version:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards__id__version
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/{id}/version
      tags:
        - boards
      x-undocumented: true
  /api/boards/adopt:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards_adopt
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards/adopt
      tags:
        - boards
      x-undocumented: true
  /api/boards:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_boards
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/boards
      tags:
        - boards
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_boards
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/boards
      tags:
        - boards
      x-undocumented: true
  /api/canva/callback:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_canva_callback
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/canva/callback
      tags:
        - canva
      x-undocumented: true
  /api/canva/connect:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_canva_connect
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/canva/connect
      tags:
        - canva
      x-undocumented: true
  /api/canva/send:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_canva_send
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/canva/send
      tags:
        - canva
      x-undocumented: true
  /api/canva/status:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_canva_status
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/canva/status
      tags:
        - canva
      x-undocumented: true
  /api/canva/templates:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_canva_templates
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/canva/templates
      tags:
        - canva
      x-undocumented: true
  /api/canva/templates/{id}:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_canva_templates__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/canva/templates/{id}
      tags:
        - canva
      x-undocumented: true
  /api/categories/{id}:
    delete:
      operationId: delete_api_categories__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Delete a category. Fails while photographs still use it.
      tags:
        - categories
  /api/categories:
    get:
      operationId: get_api_categories
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Category"
                type: array
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: Every category, in sort order.
      tags:
        - categories
    post:
      operationId: post_api_categories
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryCreate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Create a category.
      tags:
        - categories
  /api/daily-challenge/history:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_daily-challenge_history
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/daily-challenge/history
      tags:
        - daily-challenge
      x-undocumented: true
    put:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: put_api_daily-challenge_history
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PUT /api/daily-challenge/history
      tags:
        - daily-challenge
      x-undocumented: true
    delete:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: delete_api_daily-challenge_history
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: DELETE /api/daily-challenge/history
      tags:
        - daily-challenge
      x-undocumented: true
  /api/daily-challenge:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_daily-challenge
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/daily-challenge
      tags:
        - daily-challenge
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_daily-challenge
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/daily-challenge
      tags:
        - daily-challenge
      x-undocumented: true
    put:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: put_api_daily-challenge
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PUT /api/daily-challenge
      tags:
        - daily-challenge
      x-undocumented: true
  /api/elements/{id}:
    patch:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: patch_api_elements__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PATCH /api/elements/{id}
      tags:
        - elements
      x-undocumented: true
    delete:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: delete_api_elements__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: DELETE /api/elements/{id}
      tags:
        - elements
      x-undocumented: true
  /api/elements:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_elements
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/elements
      tags:
        - elements
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_elements
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/elements
      tags:
        - elements
      x-undocumented: true
  /api/fal/library:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_fal_library
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/fal/library
      tags:
        - fal
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_fal_library
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/fal/library
      tags:
        - fal
      x-undocumented: true
  /api/framer/page:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_framer_page
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/framer/page
      tags:
        - framer
      x-undocumented: true
  /api/google/picker-config:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_google_picker-config
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/google/picker-config
      tags:
        - google
      x-undocumented: true
  /api/manifest:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_manifest
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/manifest
      tags:
        - manifest
      x-undocumented: true
  /api/models/{id}:
    patch:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: patch_api_models__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: PATCH /api/models/{id}
      tags:
        - models
      x-undocumented: true
    delete:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: delete_api_models__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: DELETE /api/models/{id}
      tags:
        - models
      x-undocumented: true
  /api/models:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_models
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/models
      tags:
        - models
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_models
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/models
      tags:
        - models
      x-undocumented: true
  /api/pages/{slug}:
    get:
      operationId: get_api_pages__slug_
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: One page, with body.
      tags:
        - pages
    patch:
      operationId: patch_api_pages__slug_
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                icon:
                  anyOf:
                    - type: string
                    - type: "null"
                slug:
                  type: string
                  minLength: 1
                  maxLength: 60
                status:
                  type: string
                  enum:
                    - draft
                    - published
                title:
                  type: string
                  minLength: 1
              additionalProperties: false
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Update a page.
      tags:
        - pages
    delete:
      operationId: delete_api_pages__slug_
      parameters:
        - in: path
          name: slug
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Delete a page.
      tags:
        - pages
  /api/pages:
    get:
      description: Published pages only for a visitor; every page when a token is sent.
      operationId: get_api_pages
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/PageSummary"
                type: array
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: Page summaries for the navigation.
      tags:
        - pages
    post:
      operationId: post_api_pages
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PageCreate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Page"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Create a page.
      tags:
        - pages
  /api/photos/{id}:
    patch:
      description: "Absent keys are left alone. `exif` and `chromeUrl` are the exception: sending them empty clears the stored value, which is the only way to remove one."
      operationId: patch_api_photos__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoUpdate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Update a photograph.
      tags:
        - photos
    delete:
      operationId: delete_api_photos__id_
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Delete a photograph.
      tags:
        - photos
  /api/photos/{id}/reset:
    post:
      operationId: post_api_photos__id__reset
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Restore the pre-edit original.
      tags:
        - photos
  /api/photos/{id}/rotate:
    post:
      operationId: post_api_photos__id__rotate
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Rotate a photograph 90 degrees.
      tags:
        - photos
  /api/photos/batch-delete:
    post:
      operationId: post_api_photos_batch-delete
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoIdList"
      responses:
        "200":
          description: Success
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Delete several photographs at once.
      tags:
        - photos
  /api/photos/batch:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_photos_batch
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/photos/batch
      tags:
        - photos
      x-undocumented: true
  /api/photos:
    get:
      description: Published photographs only for a visitor. With a bearer token the unpublished ones are included, which is what the admin library reads.
      operationId: get_api_photos
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Photo"
                type: array
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: The photograph library.
      tags:
        - photos
    post:
      operationId: post_api_photos
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoCreate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Photo"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Add a photograph. Inserted at the top; every other order shifts.
      tags:
        - photos
  /api/photos/reorder:
    post:
      description: The ids in their new order.
      operationId: post_api_photos_reorder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PhotoIdList"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                items:
                  $ref: "#/components/schemas/Photo"
                type: array
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Reorder the library.
      tags:
        - photos
  /api/pinterest/board:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_pinterest_board
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/pinterest/board
      tags:
        - pinterest
      x-undocumented: true
  /api/pinterest/pin:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_pinterest_pin
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/pinterest/pin
      tags:
        - pinterest
      x-undocumented: true
  /api/robots:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_robots
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/robots
      tags:
        - robots
      x-undocumented: true
  /api/shell:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_shell
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/shell
      tags:
        - shell
      x-undocumented: true
  /api/site-settings:
    get:
      description: Falls back to the values compiled into config/sites.ts if the table is unreachable, so this never fails.
      operationId: get_api_site-settings
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: Editable branding and theme for this site.
      tags:
        - site-settings
    patch:
      operationId: patch_api_site-settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteSettingsUpdate"
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettings"
        "401":
          description: Missing or invalid token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security:
        - bearerAuth: []
      summary: Save branding and theme.
      tags:
        - site-settings
  /api/sitemap:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_sitemap
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/sitemap
      tags:
        - sitemap
      x-undocumented: true
  /api/sites/provision:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_sites_provision
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/sites/provision
      tags:
        - sites
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_sites_provision
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/sites/provision
      tags:
        - sites
      x-undocumented: true
  /api/sites/setup:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_sites_setup
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/sites/setup
      tags:
        - sites
      x-undocumented: true
  /api/unsplash/search:
    get:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: get_api_unsplash_search
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: GET /api/unsplash/search
      tags:
        - unsplash
      x-undocumented: true
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_unsplash_search
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/unsplash/search
      tags:
        - unsplash
      x-undocumented: true
  /api/upload:
    post:
      description: Not yet described in schemas/routes.ts — path and method were read from the handler.
      operationId: post_api_upload
      responses:
        "200":
          description: Success
        4XX:
          description: Request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        5XX:
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      security: []
      summary: POST /api/upload
      tags:
        - upload
      x-undocumented: true
servers:
  - description: Local
    url: http://localhost:3006
  - description: Production
    url: https://app.dallaspeters.com
