meta {
  name: DELETE categories/{id}
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/categories/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Delete a category. Fails while photographs still use it.
}
