meta {
  name: GET google/picker-config
  type: http
  seq: 1
}

GET {
  url: {{baseUrl}}/api/google/picker-config
  body: none
  auth: none
}

docs {
  GET /api/google/picker-config

  Not yet described in schemas/routes.ts — path and method were read from the handler.
}
