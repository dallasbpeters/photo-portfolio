meta {
  name: GET google/picker-config
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/google/picker-config
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  What the Google Drive picker needs to open.
}
