meta {
  name: PATCH boards/{id}
  type: http
  seq: 2
}

PATCH {
  url: {{baseUrl}}/api/boards/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "isPublic": false,
    "items": [
      ""
    ],
    "slug": "",
    "title": "",
    "wires": [
      ""
    ]
  }
}

docs {
  Save a board. Absent keys are left alone.
}
