meta {
  name: POST boards/{id}/export
  type: http
  seq: 8
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/export
  body: none
  auth: none
}

docs {
  POST /api/boards/{id}/export

  Not yet described in schemas/routes.ts — path and method were read from the handler.
}
