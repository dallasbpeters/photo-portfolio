meta {
  name: POST boards/{id}/export
  type: http
  seq: 8
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Export the board as a single image.
}
