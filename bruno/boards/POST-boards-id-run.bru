meta {
  name: POST boards/{id}/run
  type: http
  seq: 9
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/run
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "itemId": ""
  }
}

docs {
  Run a node in the board's graph.

  Runs one node and everything downstream of it. The longest request in the API — generation is measured in tens of seconds.
}
