meta {
  name: POST boards/{id}/comments
  type: http
  seq: 5
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/comments
  body: none
  auth: none
}

docs {
  POST /api/boards/{id}/comments

  Not yet described in schemas/routes.ts — path and method were read from the handler.
}
