meta {
  name: POST boards/{id}/version
  type: http
  seq: 11
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/version
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Snapshot the board so it can be restored.
}
