meta {
  name: POST boards/{id}/dataset
  type: http
  seq: 7
}

POST {
  url: {{baseUrl}}/api/boards/{{id}}/dataset
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Export the board's images and prompts as a training dataset.
}
