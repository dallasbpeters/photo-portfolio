meta {
  name: POST boards/{id}/svg
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/boards/{{id}}/svg
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "svg": ""
  }
}

docs {
  Store an SVG and return a URL for it.
}
