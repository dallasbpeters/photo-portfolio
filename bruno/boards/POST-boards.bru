meta {
  name: POST boards
  type: http
  seq: 14
}

POST {
  url: {{baseUrl}}/api/boards
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "isPublic": false,
    "title": ""
  }
}

docs {
  Create a board.
}
