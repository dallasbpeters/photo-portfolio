meta {
  name: GET boards/{id}
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/boards/{{id}}
  body: none
  auth: none
}

docs {
  One board, with items and wires.

  A published board is readable without a token; `sources` is omitted in that case.
}
