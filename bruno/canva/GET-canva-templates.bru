meta {
  name: GET canva/templates
  type: http
  seq: 5
}

GET {
  url: {{baseUrl}}/api/canva/templates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Brand templates available to the connected account.
}
