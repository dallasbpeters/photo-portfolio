meta {
  name: GET canva/templates/{id}
  type: http
  seq: 6
}

GET {
  url: {{baseUrl}}/api/canva/templates/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  One brand template, with its data fields.
}
