meta {
  name: POST ai/icon-webhook
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/ai/icon-webhook
  body: none
  auth: none
}

docs {
  Delivery callback for a queued icon generation.

  Called by fal, not by the app. Authenticated by the signature on the request rather than a bearer token.
}
