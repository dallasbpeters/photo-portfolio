meta {
  name: DELETE elements/{id}
  type: http
  seq: 2
}

DELETE {
  url: {{baseUrl}}/api/elements/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Delete an element.
}
