meta {
  name: GET sites/provision
  type: http
  seq: 1
}

GET {
  url: {{baseUrl}}/api/sites/provision
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Vercel projects this token can see.
}
