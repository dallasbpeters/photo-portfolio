meta {
  name: POST sites/provision
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/sites/provision
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "",
    "region": ""
  }
}

docs {
  Create a Vercel project and a Neon database for a new site.
}
