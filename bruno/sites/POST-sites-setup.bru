meta {
  name: POST sites/setup
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/sites/setup
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "projectId": ""
  }
}

docs {
  Run first-time setup against a provisioned site.
}
