meta {
  name: GET shell
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/shell
  body: none
  auth: none
}

docs {
  Pre-rendered HTML shell (text/html).

  Server-rendered <head> and a photograph list, so a crawler and a share card see real content rather than an empty root div.
}
