meta {
  name: GET site-settings
  type: http
  seq: 1
}

GET {
  url: {{baseUrl}}/api/site-settings
  body: none
  auth: none
}

docs {
  Editable branding and theme for this site.

  Falls back to the values compiled into config/sites.ts if the table is unreachable, so this never fails.
}
